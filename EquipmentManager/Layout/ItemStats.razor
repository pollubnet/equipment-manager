@using ItemGeneratorModels

<table class="table table-striped">
    @foreach (var itemStat in Enum.GetValues<ItemStat>())
    {
        <tr><td>@itemStat</td><td>@Item.Statistics[itemStat]</td></tr>
    }
</table>

@code {
    [Parameter] public Item Item { get; set; } = null!;
}